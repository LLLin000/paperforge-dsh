{
  "name": "paperforge-dsh",
  "version": "0.1.0",
  "private": true,
  "description": "PaperForge adapter for DeepSeek Harness (DSH).",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "typecheck": "npm run typecheck --workspaces",
    "test": "vitest run",
    "prepare": "husky"
  },
  "license": "MIT",
  "devDependencies": {
    "husky": "^9.1.7",
    "vitest": "^4.1.10"
  }
}
