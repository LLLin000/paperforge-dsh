/**
 * @paperforge/dsh
 *
 * PaperForge DSH Cordis 插件。
 *
 * 当前为骨架，后续实现：
 * - host: ctx.subprocess 调用 paperforge CLI
 * - service: ctx.paperforge
 * - tools: pf_search / pf_retrieve / pf_paper_context / pf_status / pf_action / pf_setup / pf_config
 * - client: DSH ui-slots 右侧 PaperForge 面板
 */

export const name = 'paperforge-dsh'

export function apply() {
  // TODO: 接入 Cordis Context，注册 service / tools / client
}
